<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="westgateproject.View.WritingPage"
            Title="내 계정" Icon="settings.png">
    <ContentPage Title="새 게시물" Icon="save.png">
        <ScrollView x:Name="newPosting">
			<Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
				<Label x:Name="myIdLabel"/>
                <StackLayout Orientation="Horizontal" Spacing="0" Grid.Row="1">
                    <Button x:Name="CameraButton" Text="사진 촬영" Clicked="CameraButton_Clicked"/>
                    <Button x:Name="PictureButton" Text="Gallery" Clicked="PicturePicker_Clicked"/>
                </StackLayout>
		        <Image x:Name="PhotoImage"  Grid.Row="2"/>
                <Editor x:Name="UploadTextEditor" Keyboard="Chat" Grid.Row="3" BackgroundColor="Lime" HeightRequest="200"/>
                <Button x:Name="Upload" Text="등록" Grid.Row="4" Clicked="UploadButton_Clicked" />
            </Grid>
        </ScrollView>
    </ContentPage>
    <ContentPage Title="내 활동" Icon="list.png">
        <ScrollView>
            <StackLayout x:Name="myActivity">
            </StackLayout>
        </ScrollView>     
    </ContentPage>
    <ContentPage Title="최근 소식" Icon="advertisement.png">
        <ScrollView>
            <StackLayout x:Name="myRecent">



            
           <!-- <Label x:Name="refresh" IsVisible="false"/>
            <Label x:Name="appState" Text="" TextColor="DeepSkyBlue" FontSize="20" BackgroundColor="Azure" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" VerticalTextAlignment="Center"/>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Label Text="제작자에게&#10;e-mail 보내기" Grid.Row="0" Grid.Column="0" FontSize="20" TextColor="DeepSkyBlue" BackgroundColor="Lime" HorizontalTextAlignment="Center" HorizontalOptions="FillAndExpand" VerticalOptions="Fill">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer
                            Tapped="sendingEmail"
                            NumberOfTapsRequired="1"/>
                    </Label.GestureRecognizers>
                </Label>

                <Label Grid.Row="0" Grid.Column="1" Text="제작자 홈페이지 방문" FontSize="20" TextColor="DeepSkyBlue" BackgroundColor="Yellow" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" HorizontalOptions="FillAndExpand">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer
                            Tapped="openHomepage"
                            NumberOfTapsRequired="1"/>
                    </Label.GestureRecognizers>
                </Label>
            </Grid>-->
        
            </StackLayout>  
        </ScrollView>
    </ContentPage>
</TabbedPage>